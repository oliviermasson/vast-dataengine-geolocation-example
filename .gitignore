# --- Files containing real credentials / infrastructure details ---
# NEVER commit these files: they contain (or may contain) real AWS &
# VastDB access/secret keys as well as internal endpoints.
# Use the matching *.example files as templates.
config.yaml
config_localrun.yaml
omgeoloc-secrets.yaml
manifest.yaml

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# --- VAST DataEngine CLI build artifacts ---
build.log
*.tar
*.tar.gz

# --- Misc ---
.DS_Store
*.swp
